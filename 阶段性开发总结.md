# MaXiaoyuan_Proj01 阶段性开发总结

> 总结日期：2026-09-24  
> 当前主场景：`Assets/Scenes/SampleScene.unity`  
> 项目阶段：核心玩法、猎人 AI、摄影物理系统、照片判定与基础 UI 已完成

## 1. 项目概述

当前项目是一个建立在 Gaia 森林场景中的“猎人摄影追踪 vs 玩家躲避”原型。

- 玩家可以在地面移动或切换飞行状态，利用速度、距离和树木遮挡躲避猎人。
- 猎人会巡逻、发现玩家、接近玩家、蹲下、追焦并拍照。
- 猎人不再发射实体子弹，胜负完全由照片结果决定。
- 照片会根据快门、光圈、ISO、运动速度、相机角速度、景深、对焦、噪声、遮挡、构图比例和 MTF50 综合判断。
- 玩家累计被拍到 3 张清晰照片后失败。
- 猎人累计拍出 10 张失败照片后玩家获胜。

---

## 2. 玩家系统

### 2.1 基础移动

脚本：`Assets/Script/PlayerMove.cs`

已实现：

- 相机相对方向的 WASD 移动。
- 地面移动、空中控制和飞行使用不同速度。
- Rigidbody 驱动移动。
- Continuous 碰撞检测。
- 冻结 Rigidbody 旋转，避免玩家碰撞后翻倒。
- SphereCast 地面检测。

### 2.2 飞行与耐力

已实现：

- 双击 `Space` 进入或退出飞行。
- 飞行时：
  - `Space` 上升。
  - `Shift` 下降。
- 飞行持续消耗耐力。
- 落地后恢复耐力。
- 耐力耗尽后：
  - 强制退出飞行。
  - 暂时禁止重新起飞。
  - 进入短暂硬直。
- 背包打开、游戏结束或玩家硬直时停止移动输入。

### 2.3 当前场景主要参数

| 参数 | 当前值 |
|---|---:|
| 地面速度 `walkSpeed` | 1.5 m/s |
| 飞行速度 `flightSpeed` | 13 m/s |
| 飞行上升速度 `flightRiseSpeed` | 3 m/s |
| 飞行耐力消耗 | 20/s |

---

## 3. 第三人称相机

脚本：`Assets/Script/ThirdPersonCamera.cs`

当前实际使用的相机是 `FlyCam`。旧的 `Main Camera` 已停用。

### 3.1 越肩视角

已实现：

- 近距离越肩第三人称视角。
- 鼠标控制水平和垂直旋转。
- 相机观察点高度、肩部偏移、前视距离和 FOV 可在 Inspector 调整。
- 平滑位置跟随。
- 俯仰角限制。

### 3.2 相机碰撞

已实现：

- SphereCast 检查玩家与目标相机位置之间的障碍物。
- OverlapSphere 检查相机是否已经进入物体内部。
- 二分回退寻找安全相机位置。
- 忽略玩家自身 Collider。
- 玩家贴近树干并背对树干时，相机不会直接穿入树干。

### 3.3 玩家近距淡出

已实现：

- 相机过度接近玩家时，仅玩家相机在自己的渲染期间使用独立的半透明材质副本；结束后恢复原材质。
- 相机远离后恢复不透明，猎人拍照、测光及可见性检测始终使用原始材质。
- 支持 URP 材质的 `_BaseColor` 和普通材质的 `_Color`。

### 3.4 当前生效参数

| 参数 | 当前值 |
|---|---:|
| 相机距离 | 1.6 m |
| 观察点高度 | 0.18 m |
| 肩部偏移 | 0.35 m |
| 前视距离 | 0.55 m |
| FOV | 75° |
| 俯仰范围 | -30° ～ 72° |
| 碰撞半径 | 0.07 m |

---

## 4. 猎人模型与动画

主要脚本：

- `Assets/Script/HunterMove.cs`
- `Assets/Script/Editor/HunterVisualInstaller.cs`

### 4.1 人形模型

已完成：

- 将猎人从简单胶囊外观替换为带动画的人形模型。
- 原胶囊 Renderer 已禁用。
- 父级 CapsuleCollider 继续承担物理碰撞。
- 人形模型内部 Collider 被禁用，避免模型 Collider 与主碰撞体互相干扰。
- 模型位置已与父级碰撞体和地面重新对齐。

### 4.2 动画资源

已接入：

- `Stepping Forward`
- `Stepping Backward`
- `Crouching Modified InPlace`
- `Crouch Idle`

`Standing Up.fbx` 文件仍保留，但已按当前设计从 Animator 过渡中移除。

### 4.3 动画状态逻辑

当前流程：

1. 猎人移动时播放前进或后退动画。
2. 停止移动后播放蹲下过渡。
3. 蹲下完成后进入 `Crouch Idle`。
4. 需要再次移动时直接回到前进或后退。

已实现动画门控：

- 移动动画未准备完成时，Rigidbody 不开始移动。
- 蹲下动画未完成时，不开始正式瞄准和拍照。
- 移动由 Rigidbody 驱动，Animator Root Motion 不直接控制猎人世界坐标。

---

## 5. 猎人 AI

脚本：`Assets/Script/HunterMove.cs`

### 5.1 状态机

猎人当前包含四个状态：

1. `Search`
   - 在巡逻范围中选择随机目标。
   - 避开障碍物并持续搜索玩家。

2. `Investigate`
   - 前往玩家最后已知位置。
   - 在适合的情况下主动靠近玩家改善构图。

3. `Aim`
   - 停止移动并蹲下。
   - 持续追踪玩家。
   - 完成自动对焦。
   - 评估当前追踪误差和预计运动模糊。

4. `Cooldown`
   - 拍照后等待冷却。
   - 冷却结束后重新瞄准或调查。

### 5.2 视野系统

已实现：

- 最大视野距离。
- 水平 FOV。
- 使用与拍照一致的渲染轮廓可见比例判断发现、追踪和丢失目标，无 Collider 遮挡物同样参与。
- 按 `visibilityCheckInterval`（默认 0.25 秒）缓存检查结果，分辨率为 320×180，阈值共用 `Minimum Visible Subject Fraction`（当前 80%）。
- 真正按快门前强制重新测量；不达标时不拍照、不记失败照片，丢失目标超过宽限时间后进入调查状态。
- 玩家短暂离开视野时保留宽限时间。
- 超过宽限时间后前往最后已知位置调查。

### 5.3 巡逻与避障

已实现：

- 基于场景地面 Bounds 生成随机巡逻点。
- 前向 SphereCast 检测障碍。
- 左右绕行方向选择。
- 绕行方向短时间锁定，减少左右抖动。
- OverlapSphere 分离，避免卡入障碍物。
- 卡住检测，长时间不前进时切换绕行方向。

### 5.4 构图接近偏好

当前逻辑：

- 如果玩家未飞行，或者玩家基本静止：
  - 猎人优先靠近到玩家约占画面 20%。
  - 达到偏好比例后再进入蹲下瞄准。
- 如果玩家正在飞行并且正在移动：
  - 猎人直接进入追焦拍摄，不等待构图比例。
- 该 20% 只是猎人行为偏好，不是能否拍照的硬门槛。

Inspector 可调参数：

- `Preferred Frame Coverage`
- `Stationary Approach Speed Threshold`

### 5.5 当前移动速度

| 行为 | 速度 |
|---|---:|
| 巡逻基础速度 | 1.0 m/s（SampleScene 当前值） |
| 调查/接近速度 | `patrolSpeed × investigateSpeedMultiplier` |
| 调查倍率 | 1.15 |
| 当前调查速度 | 约 1.15 m/s |

---

## 6. 猎人追焦与自动对焦

这两个系统相互独立。

### 6.1 追焦

追焦指镜头朝向对玩家的跟踪。

已实现：

- 玩家进入视野后持续追踪。
- 追踪不是完美锁定。
- 镜头会在落后目标和转过目标之间摆动。
- 摆动采用正弦振荡。
- 摆动幅度随瞄准时间逐渐减小。
- 最终仍保留残余误差，不会永久完全锁定。
- 玩家移动越快，残余追踪误差越大。
- 水平和垂直方向使用不同振荡比例。
- 相机实际角速度会进入运动模糊计算。

### 6.2 自动对焦

自动对焦指光学焦平面距离的变化。

已实现：

- 对焦目标使用玩家 Renderer 包围盒中心附近的可见表面。
- 从猎人眼睛位置向玩家方向 Raycast，获取玩家 Collider 最近表面。
- 对焦距离以指数方式趋近目标距离。
- 对焦速度由 `Autofocus Time` 控制。
- 自动对焦达到距离容差后才允许进入拍照准备。
- 焦距计算包含拍照相机相对猎人眼睛的光心偏移修正。

### 6.3 当前拍照准备条件

猎人需要同时满足：

- 蹲下动画完成。
- 玩家仍在视野中。
- 自动对焦达到容差。
- 当前追踪角度误差低于允许值。
- 预计玩家运动拖影低于准备上限。
- 条件持续满足一定时间。

如果条件短暂失效，准备进度缓慢回退，而不是立即完全清零。

---

## 7. 树木与场景碰撞

### 7.1 树木碰撞

已完成：

- `PW_Tree_Spruce_01` 分支对象配置了 BoxCollider。
- 场景中手动树木分支 Collider 会参与：
  - 玩家碰撞。
  - 相机碰撞。
  - 猎人避障。
  - 猎人视野。
  - 照片遮挡判定。
- Gaia Terrain 的 Tree Colliders 已启用。

### 7.2 当前限制

- 树叶和枝条使用简化 BoxCollider，不会与视觉叶片轮廓完全一致。
- 因此遮挡结果可能比实际画面略保守或略宽松。
- 照片结算已改为渲染轮廓比较；猎人发现和追踪玩家的视线检测仍使用 Collider。

---

## 8. 拍照系统

脚本：`Assets/Script/TakePhoto.cs`

### 8.1 拍照流程

猎人执行拍照时：

1. 创建或复用离屏 Photo Camera。
2. 根据 200mm 全画幅镜头配置视角。
3. 实时测量当前拍摄位置的场景亮度。
4. 解析 P/A/S/M 和 Auto ISO 设置。
5. 计算运动模糊、曝光、噪声、景深和对焦结果。
6. 暂时隐藏猎人自身 Renderer。
7. 根据快门时间和光圈孔径执行多次采样渲染。
8. 累积采样像素。
9. 应用曝光、噪声和可选降噪。
10. 从最终成片中测量玩家区域 MTF50。
11. 判断遮挡、构图、曝光、噪声和清晰度。
12. 保存照片和全部元数据。

### 8.2 输出

| 参数 | 当前值 |
|---|---:|
| 照片分辨率 | 1280×720 |
| 最大保存数量 | 30 |
| 传感器基准 | 36×24mm 全画幅 |
| 输出画幅 | 16:9 中央裁切 |
| 镜头焦距 | 200mm |
| URP 照片后处理 | 默认关闭 |
| 自定义物理景深 | 默认开启 |
| 景深孔径采样 | 8 次 |

---

## 9. 运动模糊

脚本：`Assets/Script/PhotoBlurPhysics.cs`

### 9.1 计算输入

运动模糊同时考虑：

- 玩家线速度。
- 猎人线速度。
- 拍照镜头角速度。
- 玩家与相机距离。
- 镜头焦距。
- 快门时间。
- 传感器像素间距。
- 画面裁切尺寸。
- 校准倍率。

### 9.2 相对运动

系统计算玩家相对相机的角速度，并区分：

- 玩家横向移动。
- 相机平移。
- 相机俯仰。
- 相机偏航。
- 相机滚转。
- Panning 对玩家相对运动的抵消。

### 9.3 多采样曝光

- 根据预计拖影长度决定渲染采样数量。
- 每约 2px 拖影增加一个采样。
- 采样数量限制在 2～24 次。
- 每次采样会改变：
  - 相机位置。
  - 相机旋转。
  - 玩家在曝光过程中的位置。
- 所有采样最终平均形成照片。

该方式不是普通屏幕空间 Motion Blur，而是根据曝光时间重建运动轨迹。

---

## 10. 光圈与物理景深

脚本：

- `Assets/Script/PhotoDepthOfFieldPhysics.cs`
- `Assets/Script/TakePhoto.cs`

### 10.1 景深物理

已实现：

- 超焦距。
- 景深近界。
- 景深远界。
- 离焦弥散圆。
- 离焦模糊像素直径。
- 离焦对 MTF50 的影响。

### 10.2 自定义景深渲染

原先使用 URP Bokeh Depth of Field，但测试发现照片即使静止也容易出现不合理的整体发糊。

当前已改为：

- 默认关闭 Photo Camera 的 URP 后处理。
- 根据 `焦距 ÷ 光圈` 计算真实孔径直径。
- 在光圈圆盘上使用 Golden Angle 分布进行多次镜头位置采样。
- 所有采样的焦平面保持重合。
- 焦平面前后的物体根据真实距离产生不同程度的散景。
- 景深采样与运动模糊采样共用渲染次数，避免简单相乘导致过高开销。

---

## 11. 曝光系统

脚本：`Assets/Script/PhotoExposurePhysics.cs`

### 11.1 曝光参数

系统包含：

- 快门速度。
- 光圈。
- ISO。
- 场景 EV100。
- 曝光补偿。
- Auto ISO。

### 11.2 曝光模式

支持：

- `P`：Program Auto。
- `A/Av`：Aperture Priority。
- `S/Tv`：Shutter Priority。
- `M`：Manual。
- `M + Auto ISO`。

### 11.3 实时测光

照片曝光不是使用全场景固定亮度。

拍摄前会：

- 从 Photo Camera 当前视角预渲染画面。
- 对画面进行中心加权亮度采样。
- 在对数亮度域计算实时 EV100。
- 根据拍摄位置和朝向解析自动曝光参数。

因此玩家处于明处、阴影、树下或逆光环境时会得到不同曝光。

### 11.4 标准 1/3 档

光圈和 ISO 已限制为常用 1/3 档序列。

光圈示例：

`f/1.0, 1.1, 1.2, 1.4, 1.6, 1.8, 2.0, 2.2, 2.5, 2.8, 3.2 ... 22`

ISO 示例：

`100, 125, 160, 200, 250, 320, 400, 500, 640 ... 12800`

自动曝光结果和 Inspector 手动值都会吸附到最近标准档位。

---

## 12. 噪声与降噪

### 12.1 噪声模型

噪声计算考虑：

- 传感器实际曝光。
- ISO 增益。
- 场景亮度。
- Shot Noise。
- 彩色随机噪声。

### 12.2 两种模式

#### 不降噪

照片需要同时满足：

- 最低 SNR。
- 最低 MTF50。

#### 降噪

- 对照片应用 3×3 邻域降噪。
- 不再使用 SNR 作为直接失败条件。
- 主要检查降噪后的 MTF50 保留率。

---

## 13. MTF50 清晰度判定

### 13.1 理论预测

理论 MTF50 综合：

- 运动拖影 MTF。
- 景深离焦 MTF。
- 降噪后的 MTF 保留率。

### 13.2 从最终成片实测

为避免理论模型对明显模糊照片给出过高分数，已经加入最终像素实测：

1. 确定玩家在照片中的像素 ROI。
2. 使用 Sobel 计算边缘梯度。
3. 选择强边缘。
4. 沿边缘法线采样 ESF/LSF。
5. 对 LSF 进行频域分析。
6. 求出 MTF50。
7. 对多条有效边缘取中值。

最终清晰度：

`最终 MTF50 保留率 = min(理论预测值, 成片实测值)`

因此理论模型不会覆盖成片中已经实际出现的模糊。

---

## 14. 构图比例

### 14.1 线性投影算法

构图比例不再使用包围框面积。

当前算法：

1. 获取玩家所有 Renderer 包围盒角点。
2. 将角点投影到传感器平面。
3. 横向按 36mm 传感器宽度换算。
4. 纵向按当前 16:9 裁切后的有效传感器高度换算。
5. 计算角点之间最大的二维传感器投影距离。
6. 除以 36mm 传感器宽度得到线性画面占比。

该算法能比面积比例更稳定地处理距离变化和斜向姿态。

### 14.2 成功阈值

照片成功所需比例位于 Hunter 的 `HunterMove` Component：

`Minimum Successful Frame Coverage`

- `0.15` 表示 15%。
- SampleScene 当前值为 0.15。
- 每张照片会记录拍摄当时使用的阈值。
- 修改参数不会追溯改变旧照片结果。

---

## 15. 遮挡判定（2026-09-24 更新）

### 15.1 基于渲染轮廓的可见比例

已用 `Assets/Script/PhotoVisibility.cs` 替换原来的 9×9 Collider 射线采样。

- 使用拍照相机的同一视角、投影、裁切与快门中点位置。
- 对玩家分别使用黑、白轮廓材质渲染，场景遮挡物保留原材质。
- 两次渲染的线性像素差值提取可见玩家轮廓，支持无 Collider 的树枝、Alpha Clip 树叶孔洞和 Alpha Blend 半透明前景。
- 参考图让玩家轮廓最后绘制且忽略深度，获得相同画面内的无遮挡轮廓；无需禁用场景遮挡物。
- 可见比例 = 可见轮廓差值总量 / 无遮挡轮廓差值总量。只统计玩家在画面内的部分，构图大小另行判断。
- 每张照片增加四次最高 640×360（16:9 时）的低分辨率渲染，复用 RenderTexture 和读回纹理。
- 判定期间关闭后处理、抗锯齿和烘焙遮挡剔除，结束后恢复相机、玩家材质及 MaterialPropertyBlock。
- 猎人自身在成片和遮挡测量期间均保持隐藏。

### 15.2 成功规则与显示

`Minimum Visible Subject Fraction = 0.8` 仍表示至少 80% 可见，即最多 20% 遮挡。

- 完全遮挡但仍位于画面内：`FAILED OCCLUDED 0%`。
- 玩家不在画面内：`FAILED NO PLAYER`。
- 放大照片同时显示 `Visible` 和 `Occluded`。
- 历史照片保留拍摄时的判定，不追溯重算。

### 15.3 验证与边界

编辑器菜单 `Tools > Photography > Check Rendered Visibility` 可在隔离预览场景运行回归检查：无遮挡、无 Collider 的完全/半遮挡、背后遮挡物、透明裁剪孔洞、半透明前景、画面外、相机背后及材质状态恢复。

当前测量采用快门中点，没有对运动模糊和光圈多采样期间的遮挡变化做积分。玩家轮廓支持普通 Mesh/SkinnedMesh 与常见 BaseMap/MainTex 裁剪；自定义顶点变形或特殊透明玩家材质需额外适配。低分辨率下的极细枝叶有采样误差。此实现仍同步读回，并非 Async GPU Readback。

---
## 16. 照片成功条件

照片需要同时满足以下所有条件：

1. 玩家遮挡比例符合要求。
2. 玩家线性画面占比符合要求。
3. 曝光 EV 在可用范围内。
4. 不降噪模式下 SNR 达标，或已启用降噪。
5. 最终 MTF50 保留率达到阈值。

任意一项失败，照片都记为失败照片。

常见失败标签：

- `FAILED NO PLAYER`
- `FAILED OCCLUDED`
- `FAILED SUBJECT`
- `FAILED EXPOSURE`
- `FAILED SNR`
- `FAILED MTF50`

---

## 17. 照片背包与 UI

### 17.1 操作

- `B`：打开或关闭照片背包。
- 点击照片：放大查看。
- `Esc`：退出放大照片。
- `R`：游戏结束后重新开始。

### 17.2 背包

已实现：

- 最多保存 30 张照片。
- 4×2 分页布局。
- 点击照片放大。
- 拍照后显示临时 Toast。

### 17.3 照片元数据

每张照片保存并显示：

- 曝光模式。
- Auto ISO 状态。
- 快门速度。
- 光圈。
- ISO。
- 对焦距离。
- MTF50。
- SNR。
- 曝光偏差。
- 运动拖影像素。
- 离焦模糊像素。
- 玩家画面占比。
- 玩家可见比例。
- 降噪状态。
- 最终成功或失败原因。

### 17.4 常驻曝光提示

照片结果始终包含：

- `OVER +x.x EV`
- `UNDER -x.x EV`
- `EXPOSURE 0.0 EV`

---

## 18. 游戏流程与胜负

脚本：`Assets/Script/GameManager.cs`

### 18.1 计数

- `Clear photos`：成功拍到玩家的清晰照片。
- `Failed photos`：没有满足全部条件的照片。

### 18.2 胜负

| 条件 | 结果 |
|---|---|
| 猎人拍到 3 张清晰照片 | 玩家失败 |
| 猎人拍出 10 张失败照片 | 玩家胜利 |

结束后：

- 玩家停止输入。
- 猎人停止正常行为。
- 显示胜负面板。
- 按 `R` 重载当前场景。

---

## 19. 当前主要脚本

| 文件 | 职责 |
|---|---|
| `PlayerMove.cs` | 玩家移动、飞行、耐力、硬直 |
| `ThirdPersonCamera.cs` | 越肩相机、碰撞、玩家淡出 |
| `HunterMove.cs` | 猎人状态机、巡逻、避障、瞄准、追焦、对焦、拍照决策 |
| `TakePhoto.cs` | 离屏拍照、照片存储、判定、背包 UI |
| `PhotoBlurPhysics.cs` | 运动模糊物理计算 |
| `PhotoExposurePhysics.cs` | 曝光模式、测光、ISO、噪声、降噪 |
| `PhotoDepthOfFieldPhysics.cs` | 景深和离焦计算 |
| `GameManager.cs` | 分数、HUD、胜负、重开 |
| `Editor/HunterVisualInstaller.cs` | 猎人人形模型和动画的编辑器安装 |

已删除或替换：

- 实体猎人子弹系统。
- 玩家受子弹命中逻辑。
- 基于子弹碰撞的胜负。
- 猎人 Root Motion 世界位移脚本。
- Standing Up 动画过渡。
- URP Bokeh 作为默认照片景深方案。

---

## 20. 当前已知限制

1. `TakePhoto` 由 Hunter 运行时动态添加，因此它自己的字段不直接保存在场景 Component 中。
2. 拍照包含多次 Render 和 `ReadPixels`，高拖影或高景深采样时开销较高。
3. 景深是多视点孔径采样近似，不是完整光线追踪。
4. MTF50 实测依赖玩家区域中存在足够有效边缘。
5. 照片遮挡已改为渲染轮廓判定；当前使用快门中点与低分辨率采样，尚未积分整个曝光过程。
6. 当前所有主要 LayerMask 接近全层检测，后续可按玩家、树木、地面、障碍分类优化。
7. 猎人 Animator Avatar 仍需持续观察 Humanoid 导入设置是否完全正确。
8. SampleScene 是当前唯一正式构建场景，测试场景未加入 Build Settings。

---

## 21. 后续可选优化方向

以下为原规划及最新状态：

- 使用低分辨率玩家遮罩替代 Collider 射线遮挡（已实现同步差分渲染版本）：
  - 渲染“包含遮挡物的玩家遮罩”。
  - 渲染“仅玩家完整遮罩”。
  - 用可见像素数除以完整玩家像素数。
  - 通过 Async GPU Readback 延后结算（待实现）。
- 将 Photo Camera 参数做成持久场景 Component。
- 使用专用 LayerMask 降低 Physics 检测成本。
- 将照片渲染、测光和 MTF50 分析异步化。
- 增加照片调试面板，实时显示每个失败条件。
- 继续校准追焦振荡、构图接近和猎人拍照频率。

---

## 22. 当前阶段结论

项目已经完成从“猎人实体射击玩家”到“猎人使用物理摄影系统拍摄玩家”的核心玩法转型。

当前版本已经具备：

- 可玩的玩家移动和飞行。
- 可调越肩第三人称相机。
- 带动画、巡逻、避障和状态机的猎人。
- 基于现实摄影参数的拍照系统。
- 运动模糊、曝光、噪声、景深和自动对焦。
- 理论与成片实测结合的 MTF50 清晰度判断。
- 构图比例和树木遮挡判定。
- 照片背包、元数据、HUD 和完整胜负循环。

目前已经形成一套可以继续进行数值平衡、性能优化和视觉完善的完整原型框架。

## 23. 猎人视觉与玩家相机修正

- 猎人发现、追踪、冷却和丢失目标改用渲染可见性，快门前强制复核；不再用 Collider 射线决定视线是否被挡住。
- 渲染视觉使用 320×180，每个候选目标默认每 0.25 秒更新，拍后结算仍为 640×360。当前为同步 GPU 读回，后续性能优化可改为异步视觉采样。
- 第三人称淡出使用独立材质副本，并由 URP beginCameraRendering/endCameraRendering 限定到玩家相机；不会改变照片的玩家透明度或深度写入。
- FlyCam 默认斜后上方视角：距离 1.6m、肩偏移 0.35m、观察点高 0.18m、初始俯角 24°、前视 0.55m、FOV 75°。保留碰撞回退和鼠标环绕。
- 验证：遮挡渲染回归、相机材质隔离回归、实际 Play Mode 下无 Collider 遮挡使可见比例归零、猎人进入 Investigate 且不拍照。测试后退出 Play Mode。
## 24. 猎人低矮障碍漏检修正

旧避障只从身体上半部发射 SphereCast，低矮树枝/石块可能挡住真实 CapsuleCollider，但避障仍返回前方 2.2m 畅通。已通过临时低矮障碍复现：旧探测返回 2.2m，身体实际碰撞距离约 0.27m。

- 前方探测改为根据身体 Bounds 构造 CapsuleCast，覆盖下半身到头部。
- 脚底保留 0.06m 探测间隙，上向法线的可通行地面不作为墙处理。
- 重叠分离改为 OverlapCapsule + ComputePenetration，按真实穿透方向脱离障碍。
- 前方斜向绕行仍被阻挡时，增加左右 90°/135°/180°候选，允许侧向绕行或后退。
- 验证：低障碍被提前发现、宽墙可选择侧向出路、地面不被误判、森林中继续正常巡逻。当前仍是局部避障，不保证复杂凹形障碍或完全包围区域的全局可达路径。